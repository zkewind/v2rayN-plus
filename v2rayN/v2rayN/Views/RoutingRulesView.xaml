<UserControl
    x:Class="v2rayN.Views.RoutingRulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:ServiceLib.Resx;assembly=ServiceLib"
    xmlns:vms="clr-namespace:ServiceLib.ViewModels;assembly=ServiceLib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Style="{StaticResource ViewGlobal}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    <DockPanel>
        <StackPanel
            Margin="{StaticResource Margin4}"
            DockPanel.Dock="Top"
            Orientation="Horizontal">
            <Button
                x:Name="btnOpenRoutingSetting"
                Command="{Binding OpenRoutingSettingCmd}"
                Style="{StaticResource DefButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon
                        Margin="{StaticResource MarginRight8}"
                        VerticalAlignment="Center"
                        Kind="SettingsOutline" />
                    <TextBlock Style="{StaticResource ToolbarTextBlock}" Text="{x:Static resx:ResUI.menuRoutingSetting}" />
                </StackPanel>
            </Button>
            <TextBlock
                Margin="{StaticResource MarginLeftRight8}"
                VerticalAlignment="Center"
                Foreground="{DynamicResource MaterialDesignBodyLight}"
                Style="{StaticResource ToolbarTextBlock}"
                Text="&#x52FE;&#x9009;&#x5DE6;&#x4FA7;&#x89C4;&#x5219;&#x96C6;&#x53EF;&#x5408;&#x5E76;&#x6D4F;&#x89C8;&#xFF0C;&#x53CC;&#x51FB;&#x6761;&#x76EE;&#x6253;&#x5F00;&#x73B0;&#x6709;&#x8DEF;&#x7531;&#x8BBE;&#x7F6E;&#x3002;" />
        </StackPanel>

        <Grid Margin="{StaticResource Margin4}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox
                Grid.Column="0"
                Header="{x:Static resx:ResUI.menuRoutingSetting}"
                Style="{StaticResource MyGroupBox}">
                <TreeView
                    x:Name="trvRoutingSets"
                    ItemsSource="{Binding TreeNodes}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vms:RoutingRulesTreeNode}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox
                                    Margin="{StaticResource MarginRight8}"
                                    VerticalAlignment="Center"
                                    IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                    Visibility="{Binding IsLeaf, Converter={StaticResource BoolToVisConverter}}" />
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style BasedOn="{StaticResource ToolbarTextBlock}" TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding Title}" />
                                    <Run Text=" " />
                                    <Run Foreground="{DynamicResource MaterialDesignBodyLight}" Text="{Binding RuleCountText}" />
                                </TextBlock>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

            <GroupBox
                Grid.Column="2"
                Header="{x:Static resx:ResUI.menuRuleList}"
                Style="{StaticResource MyGroupBox}">
                <DataGrid
                    x:Name="lstRoutingRules"
                    ItemsSource="{Binding RuleItems}"
                    SelectedItem="{Binding SelectedRuleItem}"
                    AutoGenerateColumns="False"
                    BorderThickness="1"
                    CanUserAddRows="False"
                    CanUserResizeRows="False"
                    CanUserSortColumns="False"
                    EnableRowVirtualization="True"
                    GridLinesVisibility="All"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    Style="{StaticResource DefDataGrid}">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn
                            Width="60"
                            Binding="{Binding Enabled}"
                            Header="" />
                        <DataGridTextColumn
                            Width="160"
                            Binding="{Binding RoutingRemarks}"
                            Header="&#x89C4;&#x5219;&#x96C6;" />
                        <DataGridTextColumn
                            Width="70"
                            Binding="{Binding RuleIndex}"
                            Header="#" />
                        <DataGridTextColumn
                            Width="150"
                            Binding="{Binding Remarks}"
                            Header="{x:Static resx:ResUI.LvRemarks}" />
                        <DataGridTextColumn
                            Width="100"
                            Binding="{Binding RuleTypeName}"
                            Header="{x:Static resx:ResUI.TbRuleType}" />
                        <DataGridTextColumn
                            Width="130"
                            Binding="{Binding OutboundTag}"
                            Header="outboundTag" />
                        <DataGridTextColumn
                            Width="100"
                            Binding="{Binding Port}"
                            Header="port" />
                        <DataGridTextColumn
                            Width="100"
                            Binding="{Binding Protocols}"
                            Header="protocol" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding InboundTags}"
                            Header="inboundTag" />
                        <DataGridTextColumn
                            Width="100"
                            Binding="{Binding Network}"
                            Header="network" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding MatchItems}"
                            Header="domain / ip / process" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </DockPanel>
</UserControl>
